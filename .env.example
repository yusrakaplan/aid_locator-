# Database Configuration
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here

# JWT Configuration
JWT_SECRET_KEY=your_long_random_secret_key_here_minimum_32_characters

# Spring Profile (dev, docker, production)
SPRING_PROFILES_ACTIVE=dev

# Server Configuration (optional)
SERVER_PORT=8080
FRONTEND_PORT=7200
